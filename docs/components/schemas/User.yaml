type: object
description: A generic user of the Excursions web application.

required:
  - userName
  - firstName
  - lastName
  - email
  - password

properties:
  _id:
    $ref: './BSON.yaml'
  userName:
    $ref: './Name.yaml'
  firstName:
    $ref: './Name.yaml'
  lastName:
    $ref: './Name.yaml'
  email:
    # $ref: ''
  password:
    # $ref: ''
  # excursions:
    # $ref: ''